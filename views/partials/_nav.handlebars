<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
  <a class="navbar-brand" href="/">AboutMe</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link {{#if active_home}} active {{/if}}" href="/"><i class="fa fa-home" aria-hidden="true"></i> Home<span class="sr-only">(current)</span></a>
      <a class="nav-item nav-link {{#if active_about}} active {{/if}}" href="/about"><i class="fa fa-info-circle"></i> About</a>
    </div>
    <ul class="navbar-nav ml-auto">
      {{#if user}}
      <li class="nav-item">
        <a href="/users/logout" class="nav-link {{#if active_login}} active {{/if}}"><i class="fa fa-sign-in" aria-hidden="true"></i> Logout</a>
      </li>
      {{else}}
      <li class="nav-item">
        <a href="/users/login" class="nav-link {{#if active_login}} active {{/if}}"><i class="fa fa-sign-in" aria-hidden="true"></i> Login</a>
      </li>
      <li class="nav-item">
        <a href="/users/register" class="nav-link {{#if active_register}} active {{/if}}"><i class="fa fa-user-plus"></i> Signup</a>
      </li>
      {{/if}} 
    </ul>
  </div>
</nav>